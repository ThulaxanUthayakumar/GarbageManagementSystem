namespace GMS.Domain.Enums;

public enum ImageType
{
    ComplaintEvidence = 1, // Uploaded by resident when reporting garbage
    CollectionProof = 2,   // Uploaded by collector after completing a pickup
    ProfilePhoto = 3
}
